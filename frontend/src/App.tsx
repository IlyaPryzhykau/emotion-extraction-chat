// Placeholder app shell — routing, auth, and the screens land in the next slices.
export function App() {
  return (
    <main className="page">
      <p>Reflection — frontend scaffold</p>
    </main>
  );
}
